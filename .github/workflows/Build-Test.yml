name: Build and test

# This CI workflow is trigger automatically for EVERY COMMIT or whenever a PULL REQUEST is REOPENED (open is already triggered by push)
# On PR creation, we only execute build and run tests since we don't want to release yet
# On master, the build artifacts will be used for releases 

on: 
  push: # Triggered every time on push except on master
    branches-ignore:
      - master
      - main 
  pull_request: # Trigger on PR (already triggered by push when oppened)
    types: [reopened] 
  workflow_run:  # Trigger when requested manually from Release-Manual Workflow
    workflows: [Release manual]
    types:
      - completed
    branches: [master, main]

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      project_version: ${{ steps.get_version.outputs.project_version }}

    steps: 
    - name: Checkout riseclipse-editor
      uses: actions/checkout@v2
      with:
        path: riseclipse-editor
        ref: ${{ github.ref_name }}

    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'
        cache: maven

    - name: Build with Maven 
      run: |
        cd riseclipse-editor/fr.centralesupelec.edf.riseclipse.editor
        mvn package

    - name: Extract Maven project version
      run: |
        cd riseclipse-editor/fr.centralesupelec.edf.riseclipse.editor
        echo ::set-output name=project_version::$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec)
      id: get_version

    - name: Show extracted Maven project version
      run: echo ${{ steps.get_version.outputs.project_version }}


# Future unit tests...
  # test:
  #   runs-on: ubuntu-latest
  #   needs: [build]

  #   steps: 
  #   - name: Checkout riseclipse-validator-scl2003-test
  #     uses: actions/checkout@v2
